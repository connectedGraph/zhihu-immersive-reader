# 沉浸式知乎_让知乎成为你深度阅读、外语学习、认知提升的工具
> 一叶孤舟，一卷宣纸。

将知乎网页转化为极简沉浸式阅读环境。去除广告、侧边栏和视觉噪音，专注内容本身。

适配页面：知乎首页推荐流、问答页 (`/question/`)、专栏文章页 (`/p/`)。


[![Install](https://img.shields.io/badge/安装-最新版本-brightgreen.svg)](https://greasyfork.org/zh-CN/scripts/573678)
> 把知乎从信息流平台，重新变成一个适合深度阅读、认知提升、外语学习与表达积累的工具。

知乎沉浸阅读脚本是一款面向知乎重度阅读者的用户脚本。它会把复杂、拥挤、不断分心的知乎页面，转换成更安静的卷轴式阅读环境：多主题护眼、去广告、保留必要互动、支持打印导出，并集成 AI 翻译、摘要、划词解析、表达收藏本与首页信息流 Wiki。

当前最新版已不再只服务专栏文章：它已经支持 **知乎首页推荐流、/question 问题页、单个回答页、zhuanlan 专栏文章页**。

仓库地址：[connectedGraph/zhihu-immersive-reader](https://github.com/connectedGraph/zhihu-immersive-reader)

---

## 功能概览

### 沉浸阅读

- 一键进入沉浸模式 (`Ctrl+E`)，退出时淡出动画过渡
- 5 套主题配色：宣纸、竹简、牛皮、暗血、简白，一键切换
- 智能清理文中广告、引流卡片和底部推荐
- 图片可主动折叠，点击占位符弹出全屏预览
- 保留原生操作栏（点赞、收藏）和目录导航

### 首页推荐流

- 宽幅双列卡片网格布局，支持单列/双列切换
- 每组 6 篇，上下篇切换支持跨组自动加载
- 切换动画（淡入 + 上滑）
- 待读列表 (ToRead)：收藏感兴趣的推荐内容，侧栏一键查看

### 翻译辅助

- 逐段中外对照翻译，自动注入全文摘要作为上下文
- 划词翻译 + 表达收藏本
- 兼容所有 OpenAI 格式 API（DeepSeek、中转站等）

### 阅读笔记

- 基于当前文章/回答生成结构化阅读笔记
- 支持用户先写草稿，AI 在此基础上润色和补充
- 输出包含：内容原型、深度标签、核心标签、笔记正文
- 笔记可导出为 Markdown 文件

### 零损分享

- 将当前沉浸阅读页面导出为 SVG / PNG / HTML
- 保留主题配色和排版，适合跨平台分享

### 信息流 Wiki

从首页推荐流批量构建个人学习卡片库。下面是完整 pipeline：

#### Wiki Pipeline

```
采集 → 全文抓取 → LLM 结构化 → 向量嵌入 → 持久存储 → 检索浏览
```

1. **采集**：通过知乎推荐 API 批量拉取首页内容（可配置上限，默认 100 条）
2. **全文抓取**：对每条推荐使用 `GM_xmlhttpRequest` 跨域抓取原文 HTML，解析 `initialState` 和 DOM 提取正文、问题补充、回答等结构化字段；抓取失败时回退到 API 返回的摘要
3. **LLM 学习卡片生成**：将全文送入 LLM，输出严格 JSON 结构——内容类型、一句话结论、核心知识点、可迁移场景、证据例子、入库判断、标签、可信度评估；并发度和 RPM 可配置，支持暂停/恢复
4. **向量嵌入**：调用独立配置的 Embedding API（默认 `text-embedding-3-small`），为每张卡片计算向量表示
5. **总览合成**：对有效卡片生成趋势雷达 + 学习萃取总览（可关闭）
6. **持久存储**：卡片及向量写入 IndexedDB，标签索引独立维护
7. **检索浏览**：支持按标签分类浏览、语义搜索（余弦相似度排序）；历史运行记录可回溯

整个流程在浏览器端完成，数据不离开本地。

---

## 快捷键

| 按键 | 功能 |
|------|------|
| `Ctrl+E` / `Cmd+E` | 进入/退出沉浸模式 |
| `Ctrl+H` / `Cmd+H` | 显示/隐藏侧边工具栏 |
| `T` | 展开/收起翻译面板 |
| `←` / `→` | 上一篇 / 下一篇 |

---

## 安装

1. 安装 Tampermonkey 浏览器扩展
2. 新建脚本，粘贴 `dist/bundle.user.js` 内容
3. 匹配地址设为 `*://*.zhihu.com/*`，保存即可

---

## 配置

侧栏齿轮按钮打开设置面板：

- **主 API**：Host / Key / Model（用于翻译、摘要、Wiki 卡片生成）
- **Embedding API**：独立的 Host / Key / Model（用于 Wiki 语义搜索，留空则复用主 API）
- **Wiki 参数**：最大条目数、并发数、RPM 限制、是否生成总览
- **阅读偏好**：目标翻译语言、自动摘要、自动翻译、图片自动隐藏、分享格式

---

## 开发

源码拆分为模块化文件，位于 `src/` 目录。构建单文件发布版本：

```bash
node build.js
```

输出 `dist/bundle.user.js`，可直接用于 Greasyfork 发布。

---

## 已知限制

- Wiki 仍是 Beta，可能遇到抓取失败、卡片回退、部分页面结构识别不准等问题。
- AI 功能需要 OpenAI 兼容格式 API；Claude、Gemini 原生接口需要中转或兼容层。
- API Key、缓存、表达本、Wiki 历史均为本地浏览器存储，不跨设备同步。
- 首页推荐和问题页结构会随知乎前端更新变化，若知乎改版，选择器可能需要维护。
- 自动全文翻译和 Wiki 批量摘要会消耗较多 API 额度，请按需开启。

---

## Roadmap

- 继续打磨首页 Wiki 的性能、暂停恢复体验和异常提示。
- 给表达收藏本增加更好的筛选、搜索、标签和批量管理。
- 优化移动端沉浸模式排版。
- 给更多知乎页面类型提供稳定支持。
- 探索更适合 ADHD / 长文注意力管理的阅读辅助模式。

---

## 开发动机

这个脚本最初只是为了让知乎长文读起来舒服一点。后来它逐渐长出了翻译、摘要、回答导航、首页推荐流、表达收藏本和 Wiki。

它现在更像一个小型阅读工作台：去掉干扰，保留上下文，把文章、回答和信息流重新组织成可以学习、可以复习、可以导出的东西。

如果说知乎原本是一个不断刷新、不断滑走的信息流，那么这个脚本想做的事情就是：把它重新变成一卷可以慢慢读、反复读、带走读的纸。
